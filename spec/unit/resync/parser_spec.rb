require 'spec_helper'

module Resync
  describe Parser do

    describe '#parse' do
      it 'parses an XML document'
      it 'parses an XML element'
      it 'parses a String'
      it 'fails when it gets something it can\'t parse'
    end

  end
end
